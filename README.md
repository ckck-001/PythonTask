# 13wTask

## 39
for i in range()  # 반복문 실행
if i % 2 != 0  # 2로 나눈 나머지가 0이 아님 = 홀수

## 40
if b % 3 == 0  # 3으로 나눈 나머지가 0 = 3의 배수

## 41
max(), min()  # 최대, 최소 구하기

## 42
42와 39 문제가 동일함!!

## 43
if d == 0  # 입력 받은 정수가 0이면 0 출력
for i in range(d, 1, -1)  # 입력 받은 수 부터 2 까지 반복문 

## 44
for i in range(a, b+1)  # a부터 b까지 i 할당
for j in range(a, b+1)  # a부터 b까지 j 할당

## 45
for i in a
  result += i  # a의 요소들 합 구하기
---


**keychain 충돌 문제로 인해 10주차와 12주차 과제를 새로운 github계정을 만들어 제출하게 되었습니다ㅜ**
**혹시 10주차 과제 제출 관련 문의 메일 확인해보셨을까요??ㅜ**


# 12wTask

## 32
a | b    # 합집합(union)
a & b    # 교집합(intersection)

## 33
a - b    # 차집합(difference)
a ^ b    # 대칭 차집합(symmetric difference)

## 34
b |= {100}   # update

## 35
a &= {...}   # intersection_update
a -= {...}   # difference_update
a ^= {...}   # symmetric_difference_update

## 36
a < other    # 부분집합(subset)
a > other    # 상위집합(superset)

## 37
b.add(1000)   # add
b.pop()       # pop

## 38
{x for x in range(1, 101) if 조건}  # Set Comprehension
---

# 10wTask

## 25
input()
.split()
map()
dict  # {key: value}
list()  # 변환

## 26
dict.get(key)  # 영어점수, 과학점수 가져올 때 사용

## 27
dict.fromkeys(iterable, value)  # kim의 key들을 기반으로 모든 값을 100으로 설정한 새 딕셔너리 생성

## 28
'english' in lee  # key 존재 여부 확인
del dict[key]  # 특정 키-값 삭제

## 29
dict.item()  # (key, value) 튜플 반환

## 30
if j >= 90:  # 조건문

## 31
len(dict)  # 항목 개수
